RaycastHit hit;
Ray ray = new Ray (player.position, -transform.up);

if(Physics.Raycast(ray, out hit, range))
{

	if(hit.collider.tag == "ground" && vert == 0)

	{
		rb.isKinematic = true;
	}

	else
	{
		rb.isKinematic = false;
	}

}

Debug.DrawRay (player.position, -transform.up * range, Color.green);




Vert is how my player is the name of the axis im using to move player.
vert - vertical